import type { NextConfig } from 'next';

// This application has Route Handlers, middleware, authentication, and other
// request-time server features. Do not set `output: 'export'` here: a static
// export cannot run them.
const nextConfig: NextConfig = {};

export default nextConfig;
