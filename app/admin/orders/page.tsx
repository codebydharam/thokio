import { ClipboardCheck, MoreHorizontal, Plus, Truck } from 'lucide-react';
import { AppHeader } from '@/components/AppHeader';

const stages = ['New order', 'Supplier PO sent', 'In hub QC', 'Pickup booked', 'Delivered'];

export default function AdminOrdersPage() {
  return <main className="min-h-screen bg-slate-50 text-ink"><AppHeader mode="ops" /><div className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-amber-700">Ops console / fulfillment</p><h1 className="mt-3 text-4xl font-black tracking-[-.05em] sm:text-5xl">Order workflow</h1><p className="mt-3 text-slate-500">Move every order from advance payment to a clean dispatch.</p></div><button className="inline-flex items-center gap-2 rounded-lg bg-ink px-4 py-3 text-sm font-bold text-white"><Plus size={16} />Create order</button></div><div className="mt-9 flex gap-3 overflow-x-auto pb-4 lg:grid lg:grid-cols-5 lg:overflow-visible">{stages.map((stage, index) => <section key={stage} className="min-w-[250px] rounded-2xl border border-slate-200 bg-white p-4 lg:min-w-0"><div className="flex items-start justify-between gap-2"><div><h2 className="text-sm font-bold">{stage}</h2><p className="mt-1 text-xs text-slate-400">{index === 0 ? '2 orders' : index === 2 ? '1 order' : '0 orders'}</p></div><button aria-label={`More ${stage} actions`} className="text-slate-400"><MoreHorizontal size={18} /></button></div>{index === 0 && <article className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4"><div className="flex items-center justify-between"><strong className="text-sm">THK-1048</strong><span className="rounded-full bg-white px-2 py-1 text-[10px] font-bold text-amber-700">30% paid</span></div><p className="mt-3 text-sm font-semibold">Banarasi stoles</p><p className="mt-1 text-xs text-slate-500">500 units &middot; Aarav Retail</p><button className="mt-4 w-full rounded-lg bg-ink py-2 text-xs font-bold text-white">Notify supplier</button></article>}{index === 2 && <article className="mt-5 rounded-xl border border-slate-200 p-4"><div className="flex items-center justify-between"><strong className="text-sm">THK-1046</strong><ClipboardCheck size={17} className="text-emerald-600" /></div><p className="mt-3 text-sm font-semibold">Bhadoi rugs</p><p className="mt-1 text-xs text-slate-500">QC pending &middot; 120 pieces</p><button className="mt-4 flex w-full items-center justify-center gap-2 rounded-lg bg-amber-600 py-2 text-xs font-bold text-white"><Truck size={14} />Generate pickup</button></article>}</section>)}</div></div></main>;
}
