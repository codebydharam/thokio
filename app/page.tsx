import Link from 'next/link';
import { ArrowRight, BadgePercent, ChevronRight, Clock3, MapPin, ShieldCheck, Sparkles, Truck } from 'lucide-react';
import { AppHeader } from '@/components/AppHeader';
import { ProductExplorer } from '@/components/ProductExplorer';
import { getCatalogProducts } from '@/lib/catalog-server';

export const dynamic = 'force-dynamic';

const categoryTiles = [
  ['Handloom textiles', '4.2k products', 'from-indigo-100 text-indigo-900'],
  ['Rugs & mats', '1.8k products', 'from-orange-100 text-orange-900'],
  ['Metal craft', '2.1k products', 'from-slate-200 text-slate-900'],
  ['Home & lifestyle', '3.6k products', 'from-emerald-100 text-emerald-900'],
  ['Fashion accessories', '2.7k products', 'from-rose-100 text-rose-900'],
];

export default async function HomePage() {
  const products = await getCatalogProducts();
  return <main className="min-h-screen bg-slate-50 text-ink"><AppHeader /><section className="border-b border-slate-200 bg-white"><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-amber-700">ThokIO marketplace</p><h1 className="mt-2 text-3xl font-black tracking-[-.05em] sm:text-5xl">Everything your business needs, sourced better.</h1><p className="mt-3 max-w-2xl text-slate-500">Retail-ready products and wholesale pricing from verified Indian suppliers.</p></div><Link href="/products" className="hidden items-center gap-2 rounded-xl bg-ink px-4 py-3 text-sm font-bold text-white sm:flex">Browse all products <ArrowRight size={16} /></Link></div><ProductExplorer initialProducts={products.slice(0, 4)} /></div></section><section className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><div className="grid gap-4 lg:grid-cols-[1.5fr_1fr]"><div className="relative overflow-hidden rounded-3xl bg-ink p-7 text-white sm:p-10"><div className="absolute -right-20 -top-24 size-72 rounded-full border border-indigo-400/30" /><div className="relative max-w-xl"><span className="inline-flex items-center gap-2 rounded-full bg-amber-500/15 px-3 py-1.5 text-xs font-bold text-amber-300"><Sparkles size={14} /> New buyer offer</span><h2 className="mt-8 text-4xl font-black leading-none tracking-[-.06em] sm:text-6xl">₹499 sample kit.<br /><span className="text-amber-400">₹499 back on bulk.</span></h2><p className="mt-5 max-w-md leading-7 text-indigo-200">Try three verified cluster products before you commit to a larger order.</p><Link href="/sample-kit" className="mt-7 inline-flex items-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-bold text-ink">Shop sample kit <ArrowRight size={16} /></Link></div></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-1"><div className="rounded-3xl bg-amber-100 p-6 text-amber-950"><BadgePercent size={22} /><h3 className="mt-8 text-2xl font-black tracking-[-.04em]">Wholesale prices from 25 units</h3><p className="mt-2 text-sm text-amber-900/70">Unlock tiered pricing without a long sales call.</p></div><div className="rounded-3xl bg-emerald-100 p-6 text-emerald-950"><Truck size={22} /><h3 className="mt-8 text-2xl font-black tracking-[-.04em]">Tracked delivery across India</h3><p className="mt-2 text-sm text-emerald-900/70">QC, pickup, and milestones in one buyer portal.</p></div></div></div></section><section className="border-y border-slate-200 bg-white"><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-amber-700">Shop by category</p><h2 className="mt-2 text-2xl font-black tracking-[-.04em]">Made in India, ready to move</h2></div><Link href="/products" className="hidden items-center gap-1 text-sm font-bold text-amber-700 sm:flex">See all <ChevronRight size={16} /></Link></div><div className="mt-6 flex gap-3 overflow-x-auto pb-2">{categoryTiles.map(([name, count, tone]) => <Link key={name} href={`/products?category=${encodeURIComponent(name)}`} className={`min-w-44 rounded-2xl bg-gradient-to-br ${tone} p-5 transition hover:-translate-y-1`}><p className="text-lg font-black leading-tight">{name}</p><p className="mt-9 text-xs font-semibold opacity-70">{count}</p></Link>)}</div></div></section><section className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-amber-700">Trending now</p><h2 className="mt-2 text-3xl font-black tracking-[-.05em]">Products buyers are saving</h2></div><Link href="/products" className="flex items-center gap-1 text-sm font-bold text-amber-700">View marketplace <ChevronRight size={16} /></Link></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{products.slice(0, 4).map(product => <Link key={product.id} href={`/products/${product.id}`} className="group overflow-hidden rounded-2xl border border-slate-200 bg-white transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[1.2] overflow-hidden bg-slate-100"><img src={product.image} alt={product.title} className="size-full object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded-full bg-white/90 px-2 py-1 text-[10px] font-bold">{product.source}</span></div><div className="p-4"><p className="text-xs font-semibold text-slate-400">{product.cluster}</p><h3 className="mt-2 font-bold">{product.title}</h3><div className="mt-4 flex items-center justify-between"><strong>{product.price}</strong><span className="text-xs text-slate-400">MOQ {product.moq}</span></div></div></Link>)}</div></section><section className="bg-ink text-white"><div className="mx-auto grid max-w-7xl gap-6 px-5 py-9 sm:grid-cols-3 sm:px-8"><div className="flex gap-3"><ShieldCheck className="shrink-0 text-amber-400" /><div><strong>Verified suppliers</strong><p className="mt-1 text-xs text-indigo-200">GSTIN and cluster checks before listing.</p></div></div><div className="flex gap-3"><Clock3 className="shrink-0 text-amber-400" /><div><strong>Clear lead times</strong><p className="mt-1 text-xs text-indigo-200">Know when your inventory can ship.</p></div></div><div className="flex gap-3"><MapPin className="shrink-0 text-amber-400" /><div><strong>Made closer</strong><p className="mt-1 text-xs text-indigo-200">Source directly from Indian makers.</p></div></div></div></section></main>;
}
