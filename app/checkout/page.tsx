'use client';

import Link from 'next/link';
import { ArrowLeft, CreditCard, LockKeyhole, ShoppingBag, Truck } from 'lucide-react';
import { useState } from 'react';
import { AppHeader } from '@/components/AppHeader';
import { useCartStore } from '@/lib/cart-store';

export default function CheckoutPage() {
  const { lines, clear } = useCartStore();
  const [submitted, setSubmitted] = useState(false);
  const subtotal = lines.reduce((total, line) => total + line.price * line.quantity, 0);
  function placeOrder() { setSubmitted(true); clear(); }
  if (submitted) return <main className="min-h-screen bg-slate-50 text-ink"><AppHeader mode="buyer" /><div className="mx-auto max-w-xl px-5 py-24 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-emerald-100 text-emerald-700"><ShoppingBag size={28} /></div><h1 className="mt-6 text-4xl font-black tracking-[-.05em]">Order request received</h1><p className="mt-4 leading-7 text-slate-500">Your payment provider and supplier confirmation steps will appear here once Supabase and Razorpay are connected.</p><Link href="/buyer/dashboard" className="mt-8 inline-flex rounded-xl bg-ink px-5 py-3 font-bold text-white">View my orders</Link></div></main>;
  return <main className="min-h-screen bg-slate-50 text-ink"><AppHeader mode="buyer" /><div className="mx-auto max-w-7xl px-5 py-10 sm:px-8"><Link href="/cart" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16} /> Back to cart</Link><h1 className="mt-8 text-4xl font-black tracking-[-.05em]">Checkout</h1><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_380px]"><form onSubmit={event => { event.preventDefault(); placeOrder(); }} className="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><h2 className="text-xl font-bold">Delivery details</h2><div className="mt-6 grid gap-4 sm:grid-cols-2"><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="Full name" /><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="Phone number" /><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm sm:col-span-2" placeholder="Address line 1" /><input className="rounded-xl border border-slate-200 px-4 py-3 text-sm sm:col-span-2" placeholder="Address line 2 (optional)" /><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="City" /><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="State" /><input required className="rounded-xl border border-slate-200 px-4 py-3 text-sm" placeholder="PIN code" /></div><h2 className="mt-9 text-xl font-bold">Delivery method</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="flex cursor-pointer gap-3 rounded-xl border border-amber-300 bg-amber-50 p-4"><input type="radio" name="shipping" defaultChecked /><span><strong className="flex items-center gap-2"><Truck size={16} /> Standard</strong><small className="text-slate-500">3-7 business days</small></span></label><label className="flex cursor-pointer gap-3 rounded-xl border border-slate-200 p-4"><input type="radio" name="shipping" /><span><strong>Express</strong><small className="block text-slate-500">1-3 business days</small></span></label></div><h2 className="mt-9 text-xl font-bold">Payment</h2><div className="mt-4 flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-sm text-slate-600"><CreditCard size={20} className="text-amber-600" /> Razorpay / Stripe / PayPal adapters ready for configuration.</div><button className="mt-8 flex w-full items-center justify-center gap-2 rounded-xl bg-ink py-4 font-bold text-white"><LockKeyhole size={16} /> Place order securely</button></form><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">Order summary</h2>{lines.map(line => <div key={line.variantId} className="mt-4 flex justify-between gap-3 text-sm"><span className="text-slate-600">{line.title} × {line.quantity}</span><strong>₹{(line.price * line.quantity).toLocaleString('en-IN')}</strong></div>)}<div className="mt-6 border-t border-slate-100 pt-5"><div className="flex justify-between"><span className="text-slate-500">Subtotal</span><strong>₹{subtotal.toLocaleString('en-IN')}</strong></div><div className="mt-3 flex justify-between"><span className="text-slate-500">Tax & shipping</span><span className="text-xs text-slate-400">Calculated</span></div><div className="mt-5 flex justify-between text-lg"><strong>Total</strong><strong>₹{subtotal.toLocaleString('en-IN')}</strong></div></div></aside></div></div></main>;
}
